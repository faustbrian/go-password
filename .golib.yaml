schema_version: 1
tool_version: v1.2.0
tool_checksums_sha256: 7de6de3a14184eced8c0114bf802cb24a808f3e1592d358f6f6f3d02552d0e27
manifest:
  modules: modules.json
  packages: packages.json
evidence:
  root: .verification
mutation:
  root: .verification/mutation
  imports:
    - module: .
      archive: .verification/mutation/bootstrap/root.zip
      ledger: .verification/mutation/migration-ledger.json
api:
  baselines:
    - module: .
      mode: apidiff
      path: api/root.txt
operations:
  - module: .
    gate: conformance
    steps:
      - type: make
        makefile: verification/package.mk
        target: conformance
        timeout: 20m
  - module: .
    gate: docs
    steps:
      - type: make
        makefile: verification/package.mk
        target: api-docs
        timeout: 5m
  - module: .
    gate: fuzz
    steps:
      - type: go-test
        packages: ["."]
        fuzz: '^FuzzParseEncodedHash$'
        budget: 10s
        timeout: 2m
      - type: go-test
        packages: ["."]
        fuzz: '^FuzzBoundedVerify$'
        budget: 10s
        timeout: 2m
  - module: .
    gate: interoperability
    steps:
      - type: make
        makefile: verification/package.mk
        target: interoperability
        timeout: 10m
  - module: .
    gate: test
    steps:
      - type: make
        makefile: verification/package.mk
        target: boundaries
        timeout: 5m
      - type: make
        makefile: verification/package.mk
        target: portability
        timeout: 5m
      - type: make
        makefile: verification/package.mk
        target: resource
        timeout: 10m
  - module: .
    gate: benchmark
    steps:
      - type: go-test
        packages: ["./..."]
        benchmark: '^BenchmarkApproved'
        budget: 1x
        timeout: 20m
